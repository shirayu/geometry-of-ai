---
version: '3'

tasks:
  dev:
    desc: Start dev server
    cmds:
      - pnpm --filter geometry-of-ai-site dev

  build:
    desc: Build site
    cmds:
      - pnpm --filter geometry-of-ai-site build

  preview:
    desc: Preview built site
    cmds:
      - pnpm --filter geometry-of-ai-site preview
