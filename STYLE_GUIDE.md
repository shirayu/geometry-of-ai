# 執筆・編集規約

> **規範レベル**：各規則に [MUST]（違反＝修正必須）/ [SHOULD]（原則従う。逸脱は変更ログに理由を記録）/ [MAY]（任意）を付す。

## 1. 文書の性格と想定読者

- 本シリーズは「Web連載の講義録」である。**教科書ではなく講義**——語り手の視点と一貫したメタファーを持ち、厳密性は「注意事項」と適用条件の明示で担保する。
- 想定読者 [SHOULD]：学部レベルの線形代数・確率の素養がある実務者・学生。深層学習の実装経験はあるが微分幾何は未習、を基準とする。Transformerの内部知識は第6回まで前提にしない（03の TIP で既に宣言済み）。
- 各章は**途中から読まれる**前提で書く [MUST]。章内初出の専門用語は章ごとに最小限の再導入を行う（§6.1）。

## 2. 章構成の正規形

### 2.1 セクション順序 [MUST]

```md
# 第N回：タイトル ～サブタイトル～
## 注意事項
## 導入：…
（本文セクション群）
## まとめ：…
## 次回予告
## 実装ノート：…
## 参考文献
```

例外を作る場合（第6回の「Transformerの全体像」を導入より前に置く等）は、章冒頭にその構成を選んだ理由を1文で明示する [SHOULD]。

### 2.2 まとめ・実装ノートの副題

- 両方に副題を付ける（例：「まとめ：呪いの正体」「実装ノート：高次元の性質を体感する」）

### 2.3 注意事項の書式 [SHOULD]

- 箇条書き3〜5項目。各項目は「本章の主張が成り立つ条件」または「ありがちな誤読の先回り」。
- 末尾に1行：「初読で意味が取れない項目は、読了後に再読してほしい。」を全章に追加する（注意事項は文脈なしに読むと威圧的になる。読了後に戻ることで意味が取れる書き方にする）。

## 3. コールアウト5種の使い分け [MUST]

| 種別 | 用途 | 目安 |
| --- | --- | --- |
| `[!NOTE]` | 本筋を補う事実・定義の補足（読み飛ばし可） | 章あたり6〜12 |
| `[!TIP]` | 直感・導出・覚え方・別の見方 | 章あたり1〜3 |
| `[!IMPORTANT]` | 本筋の要点で、誤解されると章全体が崩れるもの | 章あたり1〜4 |
| `[!CAUTION]` | 概念上の誤解・過剰一般化への警告（「この比喩はここまで」を含む） | 必要数 |
| `[!WARNING]` | 実装・数値計算上の事故（NaN、発散、API罠） | 実装ノート周辺のみ |

## 4. 数学記法 [MUST]

1. **ベクトル**：ラテン文字＝ `\mathbf{x}`、ギリシャ文字＝ `\boldsymbol{\mu}`。行列はラテン大文字 `\mathbf{W}`。スカラーは素のイタリック。
2. **予約記号**（詳細は `記号表.md`）：$d$ 次元、$n$ データ数、$\kappa$ 集中度、$\tau$ 温度、$m$ マージン、$\mathbb{S}^{d-1}$ 単位球面、$\Delta^{K-1}$ 確率単体、$D_{\text{KL}}$、$\mathbb{E}$。同一記号の二重使用が避けられない場合は使用箇所で明示的に断る。
3. インライン数式は前後に半角スペース（` $x$ `）。Webレンダリング崩れ防止。
4. 数式は「読める」ことを優先し、本文1段落につき表示数式は原則1本まで [SHOULD]。連続する導出は TIP コラムか実装ノートへ。

## 5. 用語

### 5.1 コア用語表 [MUST]

| 用語 | 採用 | 排除 | 備考 |
| --- | --- | --- | --- |
| 球面 | 球面 | 超球面 | 初出時に一度だけ「（高次元では超球面 hypersphere とも呼ぶ）」と注記。nGPT論文名への言及は例外 |
| 確率単体 | 単体 | シンプレックス | 章内初出は「確率単体（シンプレックス、simplex）」 |
| 埋め込み | 埋め込み | エンベディング | |
| Attention | Attention | アテンション、注意機構 | 初出に「（注意機構）」併記可 |
| vMF分布 | von Mises-Fisher（vMF）分布→以降 vMF分布 | フォン・ミーゼス | 章ごとに初出展開 |
| 測地線 | 測地線 | — | 比喩的用法には§7.5のルール適用 |

### 5.2 κ の訳語

- 二語使い分け：数学的パラメータとして語るとき「集中度」、モデルの出力の解釈として語るとき「確信度」。第7回冒頭で使い分けを1段落で宣言

## 6. 表記・約物

### 6.1 漢字のひらき [MUST]（実測した揺れに基づく）

| ひらく（採用） | とじる（排除） | 現状 |
| --- | --- | --- |
| すべて | 全て | すべて29 vs 全て2 → 多数派に統一 |
| さまざま | 様々 | 様々5 vs さまざま0 → **少数派の「さまざま」を採用**（技術文書の慣行。逆に「様々」統一でも可だが、他のひらき方針と整合させるならひらく） |
| したがって | 従って | したがって16 vs 従って1 |
| できる／こと | 出来る／事 | 揺れなし（現状維持） |
| とき（条件節） | 時 | 「〜するとき」「〜のとき」はひらく。「実装時」「計算時」など複合語は漢字可 |
| ほか | 他 | 単独用法（「ほかに」「ほかの」）はひらく。「その他」は慣用句として許容 |

将来的にlinterを導入予定

### 6.2 約物 （波ダッシュ）

- タイトルの「～」（U+FF5E）と本文の「〜」（U+301C）を混在させず、 U+FF5E「～」に全統一
- 将来的にlinterを導入予定

### 6.3 その他 [MUST]

- 英数字は半角。単位・% も半角。
- 句読点は「、。」。カンマ・ピリオドは数式と欧文のみ。
- 全角ダッシュは「——」（2連、U+2014×2）で統一。ただし使用頻度は§7.3で制限。
- 箇条書き記号は `-`（`*` 禁止）。

## 7. 文体: AI執筆特有の癖の排除を含む

AI支援執筆に典型的なパターンを避けて読みやすさを向上させる。
以下は「悪文の禁止」ではなく**頻度の上限管理**である。
どの装置も単発では有効で、効くのは希少だからこそ、という発想で運用する。

### 7.1 基本 [MUST]

- 「である」調。読者への依頼のみ「〜してほしい」を許可。
- 一文は原則80字以内。100字を超える文は分割を検討（通読時に洗い出す）。
- 1段落は1主張。3〜5文を目安とし、1文段落の連打を避ける。

### 7.2 ボールドの抑制

AI執筆の最も目立つ癖。すべてが強調されると何も強調されない。

用途を2つに限定：(1) 用語の章内初出、(2) 段落の中心主張（**1段落に最大1箇所**）。
強調が機能を取り戻す。流し読み時に「太字だけ拾えば章の骨格になる」状態を作れる。

### 7.3 構文の癖の上限管理 [SHOULD]

| パターン | 規則 | 理由 |
| --- | --- | --- |
| 対比構文「AではなくB」 | **1節（##）に1回まで**。連続使用禁止。代替：「Bである。Aと混同されやすいが〜」と肯定形から書く | AI文の代表的な型。多用すると論旨がすべて二項対立に見え、単調になる |
| 挿入ダッシュ「——」 | 1節に2回まで。括弧書き・読点・文の分割で代替 | 呼吸の長い文を量産し、読者の負荷を上げる |
| 鉤括弧による強調「〜」 | 比喩・新概念の初出のみ。2回目以降は外す（例：「霧」は初出のみ括弧、以降は裸で使う） | 括弧強調の常用は「書き手が語の責任を取らない」印象を与える |
| 「本質」「重要なのは」 | 「本質」はシリーズ通して各章2回まで。「重要なのは」は原則 IMPORTANT コールアウトに置換 | 強調語のインフレ。乱用すると本当に本質的な箇所が埋もれる |
| 修辞疑問「〜だろうか」 | 章に1回程度を上限とする。**直後に必ず答えを書く** | 増殖させない予防線 |
| 三項羅列「X、Y、Z」 | 羅列が3連続する段落を禁止。羅列のうち1つを選んで掘るか、表に逃がす | AI文は並列を3つ作りたがる。網羅感は出るが論旨が薄まる |

### 7.4 AI臭排除の最終チェックリスト [MUST]（章をまたいだ通読時に適用）

- [ ] 無内容な予告文・まとめ文（「以下で詳しく見ていこう」「ここまでをまとめると」だけの文）を削除。見出しがその役割を果たしている。
- [ ] 段落冒頭の接続詞依存：「しかし」「つまり」「さらに」で始まる段落が3連続したら書き換え。
- [ ] ヘッジの二重化（「〜である可能性があると考えられる」）を単化。逆に、仮説を事実のように断定している箇所は§11の文体マーカーに照らして修正する。
- [ ] 同一語尾の3連続（「〜である。〜である。〜である。」）を解消。
- [ ] 比喩の安売り：1段落に新規メタファーは1つまで。基幹メタファー（§7.5）以外の使い捨て比喩は、なくても通じるなら削る。
- [ ] 「先回りした自己弁護」の過剰（「もちろん〜という反論もあるだろう。しかし〜」の頻発）を監査。反論処理は CAUTION か限界節に集約。

### 7.5 メタファー運用 [MUST]

- 基幹メタファー（地図→プラネタリウム→測量士→望遠鏡/霧）は以下の表に登録されたもののみ。新規の基幹メタファーの追加は表の更新とセット。
- メタファーには必ず「賞味期限」を書く：導入から遠くない位置に、どこで比喩が破れるかを CAUTION などで明示する。
- 同一の語が章によって別対象を指す場合（「星」＝トークン/概念）、章内初出で対象を再宣言する。

| メタファー | 初出章 | 指す数学的対象 | 限界の明示 |
| --- | --- | --- | --- |
| 地図 | 第0回 | 局所座標系・多様体の局所線形近似 | 「地図は平らだが地球は丸い」——大域的構造を捉えられない点 |
| プラネタリウム/地球儀 | 第3回 | 球面埋め込み・vMF分布 | 球面は均一な曲率を持つ特殊ケース。一般多様体には適用不可 |
| 測量士 | 第6回 | Attentionによる動的な計量・座標変換 | 測量は局所的。Attentionのグローバルな相互参照を完全には捉えない |
| 望遠鏡/霧 | 第7回 | 分布埋め込みにおける不確実性・κの有限性 | 「霧」は等方的な不確実性の比喩。異方的な分散（共分散行列）は表現できない |

## 8. 図版 [SHOULD]

- 形式：SVG。ファイル名 `NN_スネークケース.svg`（`NN_fig_` 形式は廃止、既存3点はリネーム）。
- キャプション：`<img>` の直後に「図: 〜。」で始まる1〜3文。本文から図を参照するときは「図: 多様体の局所と大域の構造」のようにキャプション先頭句で指す（通し番号は採らない——章の加筆で番号管理が破綻しやすいため）。現存する「図1では」表記は削除対象。
- 配色・フォントは以下の表に従う。
- alt属性はキャプション先頭句と一致させる [MUST]。

### 図版スタイル定義

| 項目 | 値 | 備考 |
| --- | --- | --- |
| フォント（数式図） | `'STIX Two Math', 'Latin Modern Math', 'Cambria Math', serif` | 04系 |
| フォント（説明図） | `Arial, sans-serif` | 00系 |
| 本文色 | `#1a1a2e`（濃紺）/ `#333333`（ダークグレー） | 図の性格に合わせて選択 |
| サブテキスト色 | `#495057`、`#555` | キャプション・注釈 |
| アクセント赤 | `#e74c3c`（境界・警告）、`#ff6b6b`（ソフト） | |
| アクセント青 | `#3498db`（データ点）、`#2060a0`（輪郭） | |
| アクセント緑 | `#2ecc71`（中心・代表点） | |
| アクセント黄 | `#ffd43b` | |
| 背景塗り | `#f8f9fa`（グリッド）、`#dceefb`（単体内部） | |
| グリッド線 | `#dee2e6`（stroke-width 1） | |
| タイトル文字サイズ | `16px`、`font-weight: 600` | |
| 本文ラベル | `14px` | |
| 注釈 | `11.5px`〜`12px` | |
| 矢印マーカー | `fill: #333333`、refX=9 refY=3、polygon `10,3 0,6 0,0` | 00系の定義を流用 |

## 9. コード [MUST]

- 各ブロックは自己完結（import含む）か、依存する場合「※上のコードの続き」を1行目コメントで明示。
- 乱数は `seed` 固定。コメントの「期待される結果」は実際に実行して得た実測値のみ記載。
- 変数名は記号表と対応（`kappa`, `tau`, `d` 等）。
- コメントは「何をしているか」ではなく「なぜそうするか（幾何学的にどういう操作か）」を書く。

## 10. 参考文献 [MUST]

- 書式：著者・年・誌名/会議・DOI/arXivリンク・1〜2行の注釈。以下の形式を正規形とする。

```md
- 著者名, I. (年). タイトル. *誌名/会議名*, 巻(号), ページ. DOI: [xx.xxxx/xxx](https://doi.org/xx.xxxx/xxx)
    - 本章のどの議論を支える文献か（1〜2文）。
```

- 著者は全員記載。**7名以上のときのみ**筆頭+et al. 可。
- 注釈には「本章のどの議論を支える文献か」を必ず含める。
- 同一文献が複数章に出る場合、書誌は完全一致、注釈は章ごとの文脈に合わせて書き分けてよい。

## 11. 「仮説」と「事実」の文体マーカー [MUST]

本シリーズの信頼性の核。次の語尾を使い分ける：

| 主張の種別 | 語尾の例 |
| --- | --- |
| 定理・証明済み | 「〜である」「〜が成り立つ」 |
| 実験的に報告 | 「〜と報告されている」「〜を実験的に示した」＋出典 |
| 本講義の解釈 | 「〜と解釈できる」「〜と見なせる」 |
| 仮説・推測 | 「〜という仮説が立つ」「〜かもしれない」、または見出し・コラムで（仮説）と明示 |

技術レビュー時に種別と語尾の不一致を検出する。疑わしい箇所は「（要確認：主張の種別）」とコメントを残して保留する。
