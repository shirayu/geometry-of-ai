# 執筆・編集規約

## 章を書くとき

### 構成順序

```md
# 第N回：タイトル ～サブタイトル～
## 注意事項
## 導入：…
（本文セクション群）
## まとめ：…
## 次回予告
## 実装ノート：…
## 参考文献
```

「まとめ」と「実装ノート」には副題を付ける（例：「まとめ：呪いの正体」「実装ノート：高次元の性質を体感する」）。

例外的な順序を採る場合（第6回の「Transformerの全体像」を導入より前に置く等）は、原則として章冒頭にその理由を1文で明示する。

### 注意事項の書き方

箇条書き3〜5項目。各項目は「本章の主張が成り立つ条件」または「ありがちな誤読の先回り」。末尾に必ず1行追加する：

> 初読で意味が取れない項目は、読了後に再読してほしい。

注意事項は文脈なしに読むと威圧的になる。読了後に戻ることで意味が取れる書き方にする。

### 想定読者

学部レベルの線形代数・確率の素養がある実務者・学生。深層学習の実装経験はあるが微分幾何は未習、を基準とする。Transformerの内部知識は第6回まで前提にしない（03の TIP で既に宣言済み）。

各章は途中から読まれる前提で書く。章内初出の専門用語は章ごとに最小限の再導入を行う。

## 何を書くか

### コールアウト5種の使い分け

| 種別 | 用途 | 目安 |
| --- | --- | --- |
| `[!NOTE]` | 本筋を補う事実・定義の補足（読み飛ばし可） | 章あたり6〜12 |
| `[!TIP]` | 直感・導出・覚え方・別の見方 | 章あたり1〜3 |
| `[!IMPORTANT]` | 本筋の要点で、誤解されると章全体が崩れるもの | 章あたり1〜4 |
| `[!CAUTION]` | 概念上の誤解・過剰一般化への警告（「この比喩はここまで」を含む） | 必要数 |
| `[!WARNING]` | 実装・数値計算上の事故（NaN、発散、API罠） | 実装ノート周辺のみ |

### 用語・数学記法

機械検証は `task lint:terms`（`scripts/check_terms.py` + `style/terms.yaml`）で実施する。

#### 用語一覧

禁止語のうち `style/terms.yaml` に登録済みのものは `task lint:terms` で自動検証される。

| 概念 | 採用 | 排除 | 初出の形式 |
| --- | --- | --- | --- |
| 単位球面 | 球面 | 超球面 | 球面 $\mathbb{S}^{d-1}$ |
| 確率単体 | 単体 | シンプレックス | 確率単体（シンプレックス、probability simplex） |
| von Mises-Fisher 分布 | vMF 分布 | フォン・ミーゼス、vmf | von Mises-Fisher（vMF）分布 |
| 測地線距離 | 測地線距離 | 大円距離 | — |
| κ（数学的パラメータ） | 集中度 | — | — |
| κ（モデル出力の解釈） | 確信度 | — | — |
| KL ダイバージェンス | $D_{\text{KL}}$ | — | — |
| 期待値 | $\mathbb{E}$ | $E[\cdot]$ | — |
| softmax 関数 | Softmax | ソフトマックス | — |
| embedding | 埋め込み | エンベディング | — |
| attention | Attention | アテンション | — |
| ガウス分布 | 正規分布 | ガウス分布 | — |

**例外・補足**:

- 超球面は nGPT 論文名への言及のみ許可
- 確率単体は章内初出のみ「シンプレックス」併記、以降は「単体」または「確率単体」
- κ の使い分けは 07.md 冒頭で宣言済み
- attention の初出に「（注意機構）」併記可
- ガウス分布は同一章内で正規分布と混在させない（linter対象外）

#### 記号表

ベクトル・行列記法：

| 記法 | 対象 | 例 |
| --- | --- | --- |
| `\mathbf{}` | ラテン文字ベクトル・行列 | $\mathbf{x}, \mathbf{z}, \mathbf{W}$ |
| `\boldsymbol{}` | ギリシャ文字ベクトル | $\boldsymbol{\mu}, \boldsymbol{\theta}$ |
| 素のイタリック | スカラー | $\kappa, d, \tau, n$ |

予約記号：

| 記号 | 意味 | 章 |
| --- | --- | --- |
| $d$ | 埋め込み次元 | 全章 |
| $n$ | データ数 | 02〜 |
| $K$ | カテゴリ数 | 04 |
| $\mathbb{R}^d$ | $d$ 次元ユークリッド空間 | 00〜 |
| $\mathbb{S}^{d-1}$ | 単位球面（`$S^{d-1}$` は使用禁止） | 03〜 |
| $\Delta^{K-1}$ | 確率単体 | 04 |
| $\boldsymbol{\mu}$ | vMF 分布の平均方向 | 03, 07 |
| $\kappa$ | vMF 分布の集中度（$\kappa \geq 0$） | 03, 07 |
| $\tau$ | Softmax の温度パラメータ | 04 |
| $\mathbb{E}$ | 期待値 | 全章 |
| $D_{\text{KL}}$ | KL ダイバージェンス | 04, 07 |
| $g_{ij}(\theta)$ | リーマン計量（一般論） | 00 |
| $F_{ij}$ | フィッシャー情報行列の成分（04.md で $g_{ij}$ との対応注記あり） | 04 |
| $m$ | ArcFace のマージン | 05 |
| $\theta$ | ベクトル間の角度（$\boldsymbol{\theta}$ とは別物） | 03, 05 |

#### インライン数式

前後に半角スペースを入れる（` $x$ ` 形式）。Web レンダリング崩れ防止。

#### 表示数式

本文 1 段落につき原則 1 本まで。連続する導出は TIP コールアウトか実装ノートへ。

### 仮説と事実の文体マーカー

本シリーズの信頼性の核。次の語尾を使い分ける：

| 主張の種別 | 語尾の例 |
| --- | --- |
| 定理・証明済み | 「〜である」「〜が成り立つ」 |
| 実験的に報告 | 「〜と報告されている」「〜を実験的に示した」＋出典 |
| 本講義の解釈 | 「〜と解釈できる」「〜と見なせる」 |
| 仮説・推測 | 「〜という仮説が立つ」「〜かもしれない」、または見出し・コラムで（仮説）と明示 |

技術レビュー時に種別と語尾の不一致を検出する。疑わしい箇所は「（要確認：主張の種別）」とコメントを残して保留する。

## 技術レビュー

数式・技術的主張・論文引用の正確性を専門家レビューの観点で検証する。本シリーズは「直感重視だが適用条件を明示する」誠実さが売りであり、誤った一般化が1つあるだけで信頼が崩れる。

**実施方針**

- 1セッション1章。レビュアーには対象章＋第0回（記法・前提の基準）を渡す。
- レビュアーは修正せず、指摘レポートのみを出す。修正は著者が判断して行う。
- 指摘には重要度を付ける：**A（誤り：修正必須）/ B（誤解を招く：要改善）/ C（より良い説明がある：任意）**。
- 草稿層（`08.md`〜`15.md`・`appendix.*.md`）のレビューは内容が固まってから実施。要注意主張は事前リスト化せずレビュアーが章を読んで自ら洗い出す。

### 観点チェックリスト

**数式の検証**

- [ ] すべての等式・近似式を導出または出典と突合。近似式は適用条件が本文に明示されているか。
- [ ] 定数・指数の正誤（例：07の典型帯導出 $r=\sqrt{d-1}\approx\sqrt{d}$、02の直交性の標準偏差 $1/\sqrt{d}$）。
- [ ] 次元の整合（$\mathbb{S}^{d-1} \subset \mathbb{R}^d$ の使い分け、単体 $\Delta^{K-1}$ の次元）。

**幾何学的厳密性**

- [ ] 「多様体」と「リーマン多様体」の区別（00の NOTE で導入済み）が後続章で守られているか。
- [ ] 「測地線」の語が比喩として使われている箇所と数学的に正確な箇所の区別。
- [ ] 「正規化＝球面への射影」と「球面上の最適化（リーマン最適化）」の混同がないか。

**引用の突合**

- [ ] 本文中の（著者, 年）がすべて参考文献リストに存在するか（逆も）。括弧は全角・`et al.,` のカンマありで統一。
- [ ] 年号・会議名の誤りチェック。
- [ ] 原論文のabstractと突合し、本文の要約が原論文の主張と一致するか確認。

**草稿層（08〜15）のレビュー方針**

要注意主張は事前リスト化せず、レビュアーが章を読んで自ら洗い出す。参考として過去に挙がったテーマ：

- 08: Neural ODEと離散ResNetの関係。「多様体上の流れ」の比喩の適用限界
- 09: 拡散モデルの熱力学的解釈。スコア関数の厳密な定義と直感的説明のギャップ
- 10: CoTの「多様体上の軌跡」解釈が比喩であることの明示
- 11: CLIPの「同一空間に整列」の条件（L2正規化前提）
- 12: 双曲空間が有効な条件（階層構造・木構造データ限定）
- 13: MoEの最新動向（2024〜2025）の記述が陳腐化していないか
- 14: パーシステントホモロジーの「何を測っているか」がタスク依存である点
- 15: 未解決問題の記述が現時点の研究状況と整合しているか

## どう書くか

### 基本文体

「である」調。読者への依頼のみ「〜してほしい」を許可。一文は原則80字以内（100字を超えたら分割を検討）。1段落は1主張、3〜5文が目安。1文段落の連打を避ける。

### ボールドの抑制

AI執筆の最も目立つ癖。すべてが強調されると何も強調されない。用途を2つに限定する：

1. 用語の章内初出
2. 段落の中心主張（1段落に最大1箇所）

「太字だけ拾えば章の骨格になる」状態が目標。

### 構文の癖の上限管理

| パターン | 規則 |
| --- | --- |
| 対比構文「AではなくB」 | 1節（##）に1回まで。連続使用禁止。代替：「Bである。Aと混同されやすいが〜」と肯定形から書く |
| 挿入ダッシュ「——」 | 地の文・見出しで使用禁止。同格・補足の挿入は括弧書き、言い換え・敷衍は句点での文分割か読点で代替 |
| 鉤括弧による強調「〜」 | 比喩・新概念の初出のみ。2回目以降は外す |
| 「本質」「重要なのは」 | 「本質」は各章2回まで。「重要なのは」は原則 IMPORTANT コールアウトに置換 |
| 修辞疑問「〜だろうか」 | 章に1回程度を上限。直後に必ず答えを書く |
| 三項羅列「X、Y、Z」 | 羅列が3連続する段落を禁止。1つを選んで掘るか、表に逃がす |

### メタファー運用

主要な基幹メタファー（空間モデルの系譜）は以下の表で管理する。「星」「星座」「旅／軌跡」を含む全登録簿、局所メタファー、章ごとの初出、混線一覧は `style/metaphor.md` を参照。新規メタファーを追加・流用する前に同ファイルで既存の用法を確認し、本表または `style/metaphor.md` の更新とセットで行う。

メタファーには必ず「賞味期限」を書く（導入から遠くない位置に、どこで比喩が破れるかを CAUTION などで明示する）。同一の語が章によって別対象を指す場合（「星」＝トークン/概念）、章内初出で対象を再宣言する。

| メタファー | 初出章 | 指す数学的対象 | 限界の明示 |
| --- | --- | --- | --- |
| 地図 | 第0回 | 局所座標系・多様体の局所線形近似 | 「地図は平らだが地球は丸い」。大域的構造を捉えられない点 |
| 地球儀 | 第0回 | 球面（多様体の代表例）・局所と大域の二重構造 | 「地図は平らだが地球は丸い」。大域的構造を捉えられない点 |
| 霧 | 第0回 | 確率分布（第0回）→不確実性・κの有限性（第7回）→拡散の中間分布（第9回） | 「霧」は等方的な不確実性の比喩。異方的な分散（共分散行列）は表現できない |
| プラネタリウム | 第3回 | 球面埋め込み・vMF分布 | 球面は均一な曲率を持つ特殊ケース。一般多様体には適用不可 |
| 測量士 | 第6回 | Attentionによる動的な計量・座標変換 | 測量は局所的。Attentionのグローバルな相互参照を完全には捉えない |
| 望遠鏡/天体観測 | 第6回 | Attentionの観測（Query=フィルター、Key=星の輝き）／観測倍率＝構造階層・Softmax温度 | 観測対象（星）の指す対象が章により変わる点に注意。望遠鏡を不確実性へ適用するのは第7回 |

### 表記・約物

- 英数字は半角。単位・% も半角。
- 句読点は「、。」。カンマ・ピリオドは数式と欧文のみ。
- 波ダッシュはU+FF5E「～」に全統一（U+301C「〜」と混在させない）。将来的にlinterを導入予定。
- 挿入ダッシュ「——」は地の文・見出しで使用禁止（詳細は「構文の癖の上限管理」参照）。
- 箇条書き記号は `-`（`*` 禁止）。

**漢字のひらき**（実測した揺れに基づく）：

| ひらく（採用） | とじる（排除） |
| --- | --- |
| すべて | 全て |
| さまざま | 様々 |
| したがって | 従って |
| とき（条件節） | 時（「実装時」「計算時」など複合語は漢字可） |
| ほか（単独用法） | 他（「その他」は慣用句として許容） |

将来的にlinterを導入予定。

## 添付物

### 図版

- 形式：SVG。ファイル名 `NN_スネークケース.svg`（`NN_fig_` 形式は廃止、既存3点はリネーム）。
- キャプション：`<img>` の直後に「図: 〜。」で始まる1〜3文。本文から図を参照するときはキャプション先頭句で指す（通し番号は採らない。章の加筆で番号管理が破綻しやすいため）。現存する「図1では」表記は削除対象。
- alt属性はキャプション先頭句と一致させる。

図版スタイル定義：

| 項目 | 値 | 備考 |
| --- | --- | --- |
| フォント（数式図） | `'STIX Two Math', 'Latin Modern Math', 'Cambria Math', serif` | 04系 |
| フォント（説明図） | `Arial, sans-serif` | 00系 |
| 本文色 | `#1a1a2e`（濃紺）/ `#333333`（ダークグレー） | 図の性格に合わせて選択 |
| サブテキスト色 | `#495057`、`#555` | キャプション・注釈 |
| アクセント赤 | `#e74c3c`（境界・警告）、`#ff6b6b`（ソフト） | |
| アクセント青 | `#3498db`（データ点）、`#2060a0`（輪郭） | |
| アクセント緑 | `#2ecc71`（中心・代表点） | |
| アクセント黄 | `#ffd43b` | |
| 背景塗り | `#f8f9fa`（グリッド）、`#dceefb`（単体内部） | |
| グリッド線 | `#dee2e6`（stroke-width 1） | |
| タイトル文字サイズ | `16px`、`font-weight: 600` | |
| 本文ラベル | `14px` | |
| 注釈 | `11.5px`〜`12px` | |
| 矢印マーカー | `fill: #333333`、refX=9 refY=3、polygon `10,3 0,6 0,0` | 00系の定義を流用 |

### コード

- 各ブロックは自己完結（import含む）か、依存する場合「※上のコードの続き」を1行目コメントで明示。
- 乱数は `seed` 固定。コメントの「期待される結果」は実際に実行して得た実測値のみ記載。
- 変数名はstyle/symbols.mdと対応（`kappa`, `tau`, `d` 等）。
- コメントは「何をしているか」ではなく「なぜそうするか（幾何学的にどういう操作か）」を書く。

### 参考文献

正規形：

```md
- 著者名, I. (年). タイトル. *誌名/会議名*, 巻(号), ページ. DOI: [xx.xxxx/xxx](https://doi.org/xx.xxxx/xxx)
    - 本章のどの議論を支える文献か（1〜2文）。
```

- 著者は全員記載。7名以上のときのみ筆頭+et al. 可。
- 注釈には「本章のどの議論を支える文献か」を必ず含める。
- 同一文献が複数章に出る場合、書誌は完全一致、注釈は章ごとの文脈に合わせて書き分けてよい。

## Appendixの正規形

Appendixは本章（第N回）の補足・深掘りに位置づけられる独立記事であり、本章の正規形とは別の構成を採る。

### 構成順序

```md
# Appendix N: タイトル

## 本Appendixの位置づけ
（本編のどの章・概念を深掘りするか。1〜3段落）
## 注意事項
（任意。数学的前提や適用条件の限定が必要な場合のみ設ける）
（本文セクション群）
## まとめ：〇〇
（副題必須。本文内容と本編への接続を1〜3段落＋表で示す）
## 実装ノート：〇〇
（任意。理論特化Appendixはなくてよい）
## 参考文献
```

### 本章との主な違い

- 「次回予告」セクションは原則なし（本編の流れに依存しないため）。ただし次のAppendixや本編章への明示的接続がある場合は「次のステップ」等の見出しで代替可
- 「注意事項」は任意（本章では必須）
- まとめには「本編との接続まとめ」を含める（表形式可）
- 「講義本編との接続まとめ」という見出し名は廃止し、「まとめ：〇〇」に統一する

## クイズ作成基準

`site/series/NN/quiz.md`（理解度チェック）の作成・レビュー基準。機械検証は `task lint:quiz`（`check_quiz.mjs` の形式チェック、`check_quiz_quality.mjs` の品質チェック）で実施する。機械検証は形式と明白な手がかりを検出する補助であり、内容妥当性・難易度・曖昧さは人間がレビューする。

### 形式（機械検証済み）

```text
### QN. 見出し

設問文（1〜3文）。

（quiz フェンス開始）
- 誤答
- [x]正解
- 誤答
- 誤答
S: /series/NN#本文の見出しslug
A: 解説（1〜3文）。
（quiz フェンス終了）
```

- 選択肢は3〜5個、正解 `[x]` は1つだけ。
- `S:`（本文リンク、1つ以上）と `A:`（解説）を必須とする。リンク先の見出しslugは実在するものを指す。
- 設問・選択肢・解説のいずれにも「本文」「前述」「次回」「第N回」など講義本体への参照語を含めない（クイズは単独で読めること）。

### 内容妥当性

クイズは用語の再生だけでなく、本文の概念を条件・仕組み・設計判断へ接続できるかを確認する。各設問には、次のいずれか1つの学習目標を対応させる。

- 概念：定義、役割、概念間の階層を説明できる
- 仕組み：なぜその現象が起きるか、入力から結果までを説明できる
- 条件：適用条件、限界、成立しない場合を判断できる
- 応用：観測された状況から、確認すべき要因や選ぶべき設計を判断できる

8問程度の章では、概念だけに偏らせず、少なくとも2問を「条件」「応用」のいずれかにする。章の内容上、これが難しい場合は、解説で適用範囲や限界を明示する。設問数が異なる章でも、この比率を目安にする。

設問作成時には、次の対応を記録または確認する。

- どの本文の主張・学習目標を測る設問か
- 正解を分ける決定的な条件は何か
- 各誤答が表す誤解、条件の取り落とし、または近接概念との混同は何か

### 誤答（ディストラクター）の設計

クイズの価値は誤答の質で決まる。以下の失敗パターンを避ける。

**1. 断定語による消去法回避**

「必ず」「完全に」「すべての」「常に」「絶対に」「自動的に」「無条件に」「保証する」「証明された」「一切」「まったく」は `check_quiz_quality.mjs` の `cuePatterns` で機械検出され、正解以外に含まれるとlintエラーになる。これらの語は内容を理解せずとも「言い過ぎ」と分かってしまうため、原則として誤答には使わない。断定性そのものが本文の典型的な誤解である場合も、誤答を「適用条件を無視している」「適用範囲を広げすぎている」などの形に言い換え、断定語だけを手がかりにできないようにする。

同義の全称量化子（「どのような〜でも」「どの〜でも」など）は機械検出対象外だが、同じ理由で避ける。例外として、対比構造の一部として使う場合（「AはXでもYになるが、BはXでもZのままである」のように両方の選択肢で使う）は許容する。

**2. 分野の常識だけで消去できる誤答**

技術内容を理解していなくても、一般常識・比喩の日常的理解・設問文中の言葉だけで正誤判定できる誤答を作らない。特に見出しや設問文が比喩（「影絵」「霧」など）を使う場合、その比喩の日常的な理解だけで解ける選択肢を混ぜないこと。

- 悪い例：「PCAを『影絵』と呼ぶ意味は？」に対し「射影方向を変えても見える構造は同じである」→影絵が見る角度で変わることは常識で分かる。
- 良い例：技術的な誤解（分散最大の方向を選んでいるから距離関係も保たれる、など）を誤答にする。

**3. 構文の非対称性による消去法回避**

正解が「Aだが（一方で）Bである」「AしうるがCとは限らない」のようなトレードオフ・条件付き構文を取り、誤答が単純な断定文のままだと、内容を読まずに「歯切れの良さ」だけで正解が推測できてしまう。正解がトレードオフ構文の場合、誤答のうち最低1つは同じ構文で正解の主張を裏返した文にする（「AはXに有利でBはYに有利だが、どちらが適切かはタスクに依存する」の裏返しは「AはXに不利でBはYに不利なので、Aを選ぶべきである」）。

**4. 意味が成立する誤答・独立した選択肢**

誤答は技術的に意味の通る文であること。正解の言い換えや、正解と両立して実質同じことを言っている誤答は作らない。また、選択肢同士の論理関係だけで正解を特定できるような組み合わせも避ける。設問の主題と無関係な話題（別の設問の正解を紛れ込ませる等）も避ける。

**5. 長さ・具体性の非対称**

正解だけが具体的で詳細、誤答が抽象的で短い、という差が極端だと「長い選択肢が正解」という消去法が成立してしまう。誤答も同程度の具体性を持たせる。文字数を機械的に一致させる必要はないが、正解だけが誤答の平均より大幅に長い場合、または正解だけが常に最長の場合は書き直す。数式や必要な条件がある場合も、誤答側に対応する具体性を持たせる。

### レビュー時のセルフチェック

各設問について、内容を理解していない想定読者の視点で以下を自問する。

- [ ] 設問文・見出しの比喩や言葉だけで消去できる誤答はないか。
- [ ] 正解だけがトレードオフ・条件付き構文で、誤答が単純断定になっていないか。
- [ ] 誤答は「よくある誤解」「条件の取り落とし」「近接概念との混同」のいずれかを表しているか。
- [ ] 選択肢同士の論理関係だけで正解を特定できないか。
- [ ] 正解・誤答の長さや具体性に極端な差がないか。
- [ ] この設問が測る学習目標と、正解を分ける決定的な条件を説明できるか。
- [ ] 8問程度の章で、概念の再生だけでなく条件・応用を問う設問が少なくとも2問あるか。
- [ ] 解説に正解の理由と、適用範囲または限界が含まれているか。

## 通読チェックリスト

章をまたいだ通読時に適用する。

- [ ] 無内容な予告文・まとめ文（「以下で詳しく見ていこう」「ここまでをまとめると」だけの文）を削除。見出しがその役割を果たしている。
- [ ] 段落冒頭の接続詞依存：「しかし」「つまり」「さらに」で始まる段落が3連続したら書き換え。
- [ ] ヘッジの二重化（「〜である可能性があると考えられる」）を単化。仮説を事実のように断定している箇所は仮説・事実マーカーに照らして修正。
- [ ] 同一語尾の3連続（「〜である。〜である。〜である。」）を解消。
- [ ] 比喩の安売り：1段落に新規メタファーは1つまで。基幹メタファー以外の使い捨て比喩は、なくても通じるなら削る。
- [ ] 「先回りした自己弁護」の過剰（「もちろん〜という反論もあるだろう。しかし〜」の頻発）を監査。反論処理は CAUTION か限界節に集約。
