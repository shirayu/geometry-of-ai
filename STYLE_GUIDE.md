# 執筆・編集規約

## 章を書くとき

### 構成順序

```md
# 第N回：タイトル ～サブタイトル～
## 注意事項
## 導入：…
（本文セクション群）
## まとめ：…
## 次回予告
## 実装ノート：…
## 参考文献
```

「まとめ」と「実装ノート」には副題を付ける（例：「まとめ：呪いの正体」「実装ノート：高次元の性質を体感する」）。

例外的な順序を採る場合（第6回の「Transformerの全体像」を導入より前に置く等）は、原則として章冒頭にその理由を1文で明示する。

### 注意事項の書き方

箇条書き3〜5項目。各項目は「本章の主張が成り立つ条件」または「ありがちな誤読の先回り」。末尾に必ず1行追加する：

> 初読で意味が取れない項目は、読了後に再読してほしい。

注意事項は文脈なしに読むと威圧的になる。読了後に戻ることで意味が取れる書き方にする。

### 想定読者

学部レベルの線形代数・確率の素養がある実務者・学生。深層学習の実装経験はあるが微分幾何は未習、を基準とする。Transformerの内部知識は第6回まで前提にしない（03の TIP で既に宣言済み）。

各章は途中から読まれる前提で書く。章内初出の専門用語は章ごとに最小限の再導入を行う。

## 何を書くか

### コールアウト5種の使い分け

| 種別 | 用途 | 目安 |
| --- | --- | --- |
| `[!NOTE]` | 本筋を補う事実・定義の補足（読み飛ばし可） | 章あたり6〜12 |
| `[!TIP]` | 直感・導出・覚え方・別の見方 | 章あたり1〜3 |
| `[!IMPORTANT]` | 本筋の要点で、誤解されると章全体が崩れるもの | 章あたり1〜4 |
| `[!CAUTION]` | 概念上の誤解・過剰一般化への警告（「この比喩はここまで」を含む） | 必要数 |
| `[!WARNING]` | 実装・数値計算上の事故（NaN、発散、API罠） | 実装ノート周辺のみ |

### 用語・数学記法

詳細な用語一覧・記法規約・検証コマンドは `style/terms.md` を参照。以下はその要約。

**用語（主要なもの）**：球面（超球面は使わない）、埋め込み（エンベディング禁止）、Softmax（ソフトマックス禁止）、Attention（アテンション禁止）、vMF 分布（章ごと初出は「von Mises-Fisher（vMF）分布」）、確率単体（章内初出は「確率単体（シンプレックス、probability simplex）」）。

κ の訳語は二語使い分け：数学的パラメータとして語るとき「集中度」、モデルの出力の解釈として語るとき「確信度」。第7回冒頭で使い分けを1段落で宣言。

**数学記法**：ラテン文字ベクトル＝ `\mathbf{x}`、ギリシャ文字ベクトル＝ `\boldsymbol{\mu}`。スカラーは素のイタリック。予約記号の詳細は `style/symbols.md` を参照。インライン数式は前後に半角スペース（` $x$ `）。本文1段落につき表示数式は原則1本まで。

### 仮説と事実の文体マーカー

本シリーズの信頼性の核。次の語尾を使い分ける：

| 主張の種別 | 語尾の例 |
| --- | --- |
| 定理・証明済み | 「〜である」「〜が成り立つ」 |
| 実験的に報告 | 「〜と報告されている」「〜を実験的に示した」＋出典 |
| 本講義の解釈 | 「〜と解釈できる」「〜と見なせる」 |
| 仮説・推測 | 「〜という仮説が立つ」「〜かもしれない」、または見出し・コラムで（仮説）と明示 |

技術レビュー時に種別と語尾の不一致を検出する。疑わしい箇所は「（要確認：主張の種別）」とコメントを残して保留する。

## どう書くか

### 基本文体

「である」調。読者への依頼のみ「〜してほしい」を許可。一文は原則80字以内（100字を超えたら分割を検討）。1段落は1主張、3〜5文が目安。1文段落の連打を避ける。

### ボールドの抑制

AI執筆の最も目立つ癖。すべてが強調されると何も強調されない。用途を2つに限定する：

1. 用語の章内初出
2. 段落の中心主張（1段落に最大1箇所）

「太字だけ拾えば章の骨格になる」状態が目標。

### 構文の癖の上限管理

| パターン | 規則 |
| --- | --- |
| 対比構文「AではなくB」 | 1節（##）に1回まで。連続使用禁止。代替：「Bである。Aと混同されやすいが〜」と肯定形から書く |
| 挿入ダッシュ「——」 | 1節に2回まで。括弧書き・読点・文の分割で代替 |
| 鉤括弧による強調「〜」 | 比喩・新概念の初出のみ。2回目以降は外す |
| 「本質」「重要なのは」 | 「本質」は各章2回まで。「重要なのは」は原則 IMPORTANT コールアウトに置換 |
| 修辞疑問「〜だろうか」 | 章に1回程度を上限。直後に必ず答えを書く |
| 三項羅列「X、Y、Z」 | 羅列が3連続する段落を禁止。1つを選んで掘るか、表に逃がす |

### メタファー運用

基幹メタファーは以下の表に登録されたもののみ。新規追加は表の更新とセット。

メタファーには必ず「賞味期限」を書く——導入から遠くない位置に、どこで比喩が破れるかを CAUTION などで明示する。同一の語が章によって別対象を指す場合（「星」＝トークン/概念）、章内初出で対象を再宣言する。

| メタファー | 初出章 | 指す数学的対象 | 限界の明示 |
| --- | --- | --- | --- |
| 地図 | 第0回 | 局所座標系・多様体の局所線形近似 | 「地図は平らだが地球は丸い」——大域的構造を捉えられない点 |
| プラネタリウム/地球儀 | 第3回 | 球面埋め込み・vMF分布 | 球面は均一な曲率を持つ特殊ケース。一般多様体には適用不可 |
| 測量士 | 第6回 | Attentionによる動的な計量・座標変換 | 測量は局所的。Attentionのグローバルな相互参照を完全には捉えない |
| 望遠鏡/霧 | 第7回 | 分布埋め込みにおける不確実性・κの有限性 | 「霧」は等方的な不確実性の比喩。異方的な分散（共分散行列）は表現できない |

### 表記・約物

- 英数字は半角。単位・% も半角。
- 句読点は「、。」。カンマ・ピリオドは数式と欧文のみ。
- 波ダッシュはU+FF5E「～」に全統一（U+301C「〜」と混在させない）。将来的にlinterを導入予定。
- 全角ダッシュは「——」（2連、U+2014×2）で統一。
- 箇条書き記号は `-`（`*` 禁止）。

**漢字のひらき**（実測した揺れに基づく）：

| ひらく（採用） | とじる（排除） |
| --- | --- |
| すべて | 全て |
| さまざま | 様々 |
| したがって | 従って |
| とき（条件節） | 時（「実装時」「計算時」など複合語は漢字可） |
| ほか（単独用法） | 他（「その他」は慣用句として許容） |

将来的にlinterを導入予定。

## 添付物

### 図版

- 形式：SVG。ファイル名 `NN_スネークケース.svg`（`NN_fig_` 形式は廃止、既存3点はリネーム）。
- キャプション：`<img>` の直後に「図: 〜。」で始まる1〜3文。本文から図を参照するときはキャプション先頭句で指す（通し番号は採らない——章の加筆で番号管理が破綻しやすいため）。現存する「図1では」表記は削除対象。
- alt属性はキャプション先頭句と一致させる。

図版スタイル定義：

| 項目 | 値 | 備考 |
| --- | --- | --- |
| フォント（数式図） | `'STIX Two Math', 'Latin Modern Math', 'Cambria Math', serif` | 04系 |
| フォント（説明図） | `Arial, sans-serif` | 00系 |
| 本文色 | `#1a1a2e`（濃紺）/ `#333333`（ダークグレー） | 図の性格に合わせて選択 |
| サブテキスト色 | `#495057`、`#555` | キャプション・注釈 |
| アクセント赤 | `#e74c3c`（境界・警告）、`#ff6b6b`（ソフト） | |
| アクセント青 | `#3498db`（データ点）、`#2060a0`（輪郭） | |
| アクセント緑 | `#2ecc71`（中心・代表点） | |
| アクセント黄 | `#ffd43b` | |
| 背景塗り | `#f8f9fa`（グリッド）、`#dceefb`（単体内部） | |
| グリッド線 | `#dee2e6`（stroke-width 1） | |
| タイトル文字サイズ | `16px`、`font-weight: 600` | |
| 本文ラベル | `14px` | |
| 注釈 | `11.5px`〜`12px` | |
| 矢印マーカー | `fill: #333333`、refX=9 refY=3、polygon `10,3 0,6 0,0` | 00系の定義を流用 |

### コード

- 各ブロックは自己完結（import含む）か、依存する場合「※上のコードの続き」を1行目コメントで明示。
- 乱数は `seed` 固定。コメントの「期待される結果」は実際に実行して得た実測値のみ記載。
- 変数名はstyle/symbols.mdと対応（`kappa`, `tau`, `d` 等）。
- コメントは「何をしているか」ではなく「なぜそうするか（幾何学的にどういう操作か）」を書く。

### 参考文献

正規形：

```md
- 著者名, I. (年). タイトル. *誌名/会議名*, 巻(号), ページ. DOI: [xx.xxxx/xxx](https://doi.org/xx.xxxx/xxx)
    - 本章のどの議論を支える文献か（1〜2文）。
```

- 著者は全員記載。7名以上のときのみ筆頭+et al. 可。
- 注釈には「本章のどの議論を支える文献か」を必ず含める。
- 同一文献が複数章に出る場合、書誌は完全一致、注釈は章ごとの文脈に合わせて書き分けてよい。

## Appendixの正規形

Appendixは本章（第N回）の補足・深掘りに位置づけられる独立記事であり、本章の正規形とは別の構成を採る。

### 構成順序

```md
# Appendix N: タイトル

## 本Appendixの位置づけ
（本編のどの章・概念を深掘りするか。1〜3段落）
## 注意事項
（任意。数学的前提や適用条件の限定が必要な場合のみ設ける）
（本文セクション群）
## まとめ：〇〇
（副題必須。本文内容と本編への接続を1〜3段落＋表で示す）
## 実装ノート：〇〇
（任意。理論特化Appendixはなくてよい）
## 参考文献
```

### 本章との主な違い

- 「次回予告」セクションは原則なし（本編の流れに依存しないため）。ただし次のAppendixや本編章への明示的接続がある場合は「次のステップ」等の見出しで代替可
- 「注意事項」は任意（本章では必須）
- まとめには「本編との接続まとめ」を含める（表形式可）
- 「講義本編との接続まとめ」という見出し名は廃止し、「まとめ：〇〇」に統一する

### 既知の違反（全Appendix共通）

- appendix.1〜6：まとめ見出しを正規形（「まとめ：〇〇」）に統一
- appendix.5：参考文献セクションなし → 新設
- appendix.3：まとめが本文セクション群の後ではなく末尾に孤立 → 順序を正規形に合わせる

## 保留事項（後日判断）

### 図版（指示書03）の対象拡張

08〜15・appendixへの図版追加は **Phase 2 の時点で改めて判断する**。

作業手順：

1. 08〜15・appendixの各章を読み、「図がなければ理解困難な節」を洗い出す
2. 図版計画（仕様書）を章ごとに作成し著者確認
3. 承認後に作図

現状の指示書03は00〜07を対象としたまま運用し、08以降は別途追補として作成する。

### 指示書04（技術レビュー）の要注意主張リスト拡張

08〜15に含まれる下記テーマについて、**技術レビュー実施時に担当者が要注意箇所を自ら洗い出してレポートに含める**。著者が事前に一覧化はしない。

対象テーマ（例示）：

- 08: Neural ODEと離散ResNetの関係。「多様体上の流れ」の比喩の適用限界
- 09: 拡散モデルの熱力学的解釈。スコア関数の厳密な定義と直感的説明のギャップ
- 10: CoTの「多様体上の軌跡」解釈が比喩であることの明示
- 11: CLIPの「同一空間に整列」の条件（L2正規化前提）
- 12: 双曲空間が有効な条件（階層構造・木構造データ限定）
- 13: MoEの最新動向（2024〜2025）の記述が陳腐化していないか
- 14: パーシステントホモロジーの「何を測っているか」がタスク依存である点
- 15: 未解決問題の記述が現時点の研究状況と整合しているか

## 通読チェックリスト

章をまたいだ通読時に適用する。

- [ ] 無内容な予告文・まとめ文（「以下で詳しく見ていこう」「ここまでをまとめると」だけの文）を削除。見出しがその役割を果たしている。
- [ ] 段落冒頭の接続詞依存：「しかし」「つまり」「さらに」で始まる段落が3連続したら書き換え。
- [ ] ヘッジの二重化（「〜である可能性があると考えられる」）を単化。仮説を事実のように断定している箇所は仮説・事実マーカーに照らして修正。
- [ ] 同一語尾の3連続（「〜である。〜である。〜である。」）を解消。
- [ ] 比喩の安売り：1段落に新規メタファーは1つまで。基幹メタファー以外の使い捨て比喩は、なくても通じるなら削る。
- [ ] 「先回りした自己弁護」の過剰（「もちろん〜という反論もあるだろう。しかし〜」の頻発）を監査。反論処理は CAUTION か限界節に集約。
