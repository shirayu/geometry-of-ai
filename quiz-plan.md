# 理解度チェック設計計画

## 目的

各回の本文を読んだあとに、読者が次の内容を自分で確認できるようにする。

- その回の中心概念を説明できる
- 概念が成り立つ条件と限界を区別できる
- 現象の理由や因果関係を説明できる
- 次の設計・手法へ議論を接続できる

理解度チェックは、本文の細部を覚えているかを試す小テストではない。本文の論理を再構成し、誤解しやすい境界を確認するための再学習の場とする。

## 現状の評価

試作の [第2回の理解度チェック](site/series/02/quiz.md) は、次の点がよい。

- 必須と発展に分け、読者の負担を調整しようとしている
- Q2で独立同分布のランダムベクトルという前提を明示している
- Q3で「直交性は祝福にもなる」という理由を問うている
- Q4で次回の球面上の設計に接続している
- 正解だけでなく、誤解を防ぐ解説を表示している

一方、全回に展開する前に次を改善する必要がある。

- 「必須／発展」は重要度と難易度を混同しやすい
- 問題ごとの学習目標と本文の根拠が明示されていない
- Q5のハブ化は第2回の中心テーマであり、「発展」扱いとは合わない
- Q6の「本文が挙げていないもの」は、否定形かつ本文依存が強い
- 設問形式の誤りを検出する自動チェックがない
- 現在のクイズUIにはキーボード操作、数式表示、安全なHTML処理の課題がある

## 基本方針

### 「必須／発展」は使わない

設問を難易度で二分するのではなく、本文の理解の流れに沿ってグループ化する。

推奨するグループは次の4つである。

1. **要点をつかむ**：中心概念や基本的な現象を確認する
2. **条件を見極める**：成立条件、限界、例外を確認する
3. **仕組みを考える**：なぜ起きるのか、式や因果関係を考える
4. **設計につなぐ**：応用、設計判断、次回への接続を確認する

この分類は問題の価値を上下させるものではなく、「何を理解するための問題か」を示すものである。

設問が少ない回では、無理に4グループを表示しない。目安は1グループ2問以上、通常は1回8〜12問程度とする。内容が薄い回で問題数を水増ししない。

### 各回の設問数

標準的な構成は次の通りとする。

| グループ | 目安 | 役割 |
| --- | ---: | --- |
| 要点をつかむ | 2〜3問 | 核心概念の確認 |
| 条件を見極める | 2〜3問 | 適用条件・限界の確認 |
| 仕組みを考える | 2〜3問 | 因果・数式・構造の理解 |
| 設計につなぐ | 2〜3問 | 応用・判断・次回への接続 |

各学習目標につき、設問は最大2問を基本とする。同じ知識を言い換えただけの設問は作らない。

### 数式処理の標準

数式の表示エンジンは、本文と quiz ともに MathJax に統一する。

- Markdown上の記法は本文と同じ `$...$` と `$$...$$` を使う
- VitePress の `markdown-it-mathjax3` を標準の表示経路とする
- quiz の選択肢・解説も、本文と同じ数式処理を通す
- `katex` は既存の `check-math.mjs` による構文検証専用とする
- quiz のコンポーネント描画後に、追加された数式を MathJax が typeset できるようにする

quiz だけを KaTeX で描画して、本文と表示エンジンを分けない。数式の見た目と対応記法をシリーズ全体でそろえる。

## 設問設計の仕様

各回で、先に3〜5個の学習目標を定義する。設問は必ずいずれかの目標に対応させる。

### 設問ごとに管理する項目

- 学習目標ID
- グループ
- 本文の根拠となる節
- 本文リンク（`S: /series/ファイル#見出しslug`）
- 対象とする誤解
- 正解の理由
- 各誤答が誤りである理由
- 問う証拠の種類（定義、条件、導出、例、設計判断など）

HTMLコメントなどのメタデータとして quiz 本文に埋め込むか、別のメタデータファイルで管理する。

本文リンクはquizフェンス内に `S:` 行で記録する。各問題に1つ以上置き、表示時には本文の対応箇所へのリンクとして提示する。lintで対象ファイルの存在と見出しslugの一致を検証する。

### 問題の品質基準

- 正解は一つに定まる
- 問題文だけで必要な前提が分かる
- 本文を閉じても、問題文・選択肢・解説だけで解答を判断できる
- 正解は本文の主張または本文から妥当に導ける
- 誤答は、読者が実際にしそうな誤解になっている
- 「本文に書いてあったか」だけを問わない
- 否定形の設問をできるだけ避ける
- 数式を問う場合、適用条件と近似の意味を明示する
- 解説は正解の理由だけでなく、誤答がなぜ魅力的に見えるかも補足する
- 事実、解釈、仮説の区別を本文の文体規約とそろえる

### 問う内容のバランス

各回に、次の種類を偏りなく含める。

- 概念の説明
- 成立条件・適用限界
- 因果関係や仕組み
- 数式・定量的な判断
- 具体例への適用
- 次の章の設計方針への接続

単なる用語再生は、全体の3分の1以下を目安とする。

## 第2回への適用案

第2回は次のように再編できる。

### 要点をつかむ

- ノルムに混在していた三つの意味
- 高次元で距離が集中する現象
- 高次元でランダムベクトルがほぼ直交する理由

### 条件を見極める

- 距離の集中に必要な独立同分布の仮定
- アンビエント次元と内在次元の違い
- 学習済み埋め込みにランダムベクトルの性質をそのまま適用できない理由

### 仕組みを考える

- 距離の集中と相対的なばらつきの関係
- 内積におけるノルムとハブ化の関係
- 直交性が独立な表現の配置に役立つ理由

### 設計につなぐ

- ノルムを固定し、方向を自由度として残す設計
- カプセルネットワークの意義と実用上の限界
- 第3回の球面上の計算へ進む理由

現行Q6の「本文が挙げていないもの」は、次のような統合問題に置き換える方がよい。

> カプセルネットワークの設計意図と実用上の評価を同時に表す説明はどれか。

## 実装上の改善

### 自動検証

`site/scripts/check_quiz.mjs` を追加し、`task lint:quiz` として `task lint` に組み込む。

最低限、次を検証する。

- 正解が必ず1つだけある
- 選択肢が3〜5個ある
- 解説 `A:` が存在する
- 選択肢が重複していない
- 問題文・解説が空でない
- 学習目標が少なくとも1問で測られている
- quiz の章番号と本文・目次のリンクが一致している
- 未知の構文や誤ったメタデータがない
- quiz内の数式がMathJaxのSVGへ変換できる
- 本文参照を示す表現（「本文」「前述」「次回」など）がない

現在のパーサーは、正解指定がない場合に先頭選択肢を正解として扱い、複数の正解指定もエラーにしない。全展開前に、形式エラーとして停止するよう変更する。

### UI

[Quiz.vue](site/.vitepress/theme/Quiz.vue) について、次を検討する。

- `li` のクリック処理を、キーボード操作可能な `button` に変更する
- 正解・不正解の表示を `aria-live` で通知する
- 1グループごとの進捗を表示する
- グループを折りたたみ可能にするか検討する
- 数式を選択肢や解説に書く場合もMathJaxで処理する
- `v-html` に渡す内容の安全性を確保する
- 全問終了後の集計を追加するか検討する

点数で合否を決めるより、各グループを「理解の進み具合」として表示する方が、この講義の目的に合う。

## レビューフロー

1. 本文の学習目標を3〜5個定義する
2. 設問と本文の根拠を対応づける
3. 執筆者が正解・誤答・誤解を確認する
4. 本文を読んでいないレビュアーが、quiz 単体で問題文を理解できるか確認する
5. 本文と照合して、技術的な正確性と条件の抜けを確認する
6. 自動 lint とサイトビルドを実行する

レビューでは、各設問を次の観点で判定する。

- A：正解が誤っている、または本文と矛盾する
- B：前提不足や表現の曖昧さで誤解を招く
- C：よりよい誤答・解説・設問形式がある

AとBは公開前に修正する。Cは、学習目標の重複や読者負担を見て判断する。

## 展開手順

1. 第2回を基準例として、グループ構成とメタデータ形式を確定する
2. 第3回、第4回、第7回で試す
3. 数式中心、幾何中心、分布・不確実性中心の3種類で問題設計の揺れを確認する
4. 自動検証とUIの問題を修正する
5. レビュー基準を確定する
6. 残りの回へ展開する

## 公開前の合格条件

- 各学習目標が少なくとも1問で確認できる
- 4グループのうち、内容上必要なグループが揃っている
- 設問の正解と解説を本文から追跡できる
- 成立条件・限界を少なくとも1問扱っている
- 否定形や本文依存の trivia 問題に偏っていない
- 本文中の表・図・節を参照しないと解けない問題がない
- 自動 quiz lint が通る
- サイトビルドが通る
- A・B判定のレビュー指摘が残っていない
